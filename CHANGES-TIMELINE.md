# 타임라인·카메라 수정 및 검수

## 참조 이미지에 따른 레이아웃 재수정

- 별도 인스펙터와 중복 트랙명을 제거했습니다.
- 왼쪽에 편집 가능한 옵션명·값·키 추가 버튼, 오른쪽에 해당 옵션의 키프레임 시간 막대를 배치했습니다.
- 왼쪽 옵션과 오른쪽 키는 동일한 29px 행 간격을 사용하며 하나의 스크롤 영역에서 움직입니다.
- 기본 화면은 두 영역이고, 그래프는 요청 시 버튼으로 열 수 있습니다.
- 브라우저에서 8개 행 정렬, 값 편집과 5초 키 추가를 확인했습니다. 원격 배포는 하지 않았습니다.

## 사용
좌측 상단 지도 선택에서 **어스_G**를 선택하고, 3D 로드가 끝나면 **타임라인** 버튼을 누릅니다.

- After Effects를 참고한 중립적인 어두운 패널, 속성 트랙, 다이아몬드 키프레임, 파란 재생 헤드.
- 그래프 편집기 표시 전환, 100–800% 시간축 확대, 보기 이동.
- 키 추가/드래그/삭제, 프레임 이동, 재생/반복, 보간 곡선, 실행 취소/다시 실행, JSON 입출력 유지.
- 트랙 이름 선택 시 재생 위치가 0초로 이동하던 문제 수정.
- 화면 폭에 맞게 SVG 좌표를 갱신해 글자 가로 늘어짐 방지.

## 3D 카메라

| 입력 | 동작 |
| --- | --- |
| 우클릭 드래그 | 카메라 위치에서 시점 회전 |
| 우클릭 + W/A/S/D | 전진/왼쪽/후진/오른쪽 |
| 우클릭 + Q/E | 하강/상승 |
| 우클릭 + 휠 | 이동 속도 조절 |
| 휠 | 전진/후진 |
| 좌클릭 드래그 | 전후 이동 및 좌우 회전 |
| 가운데 버튼 또는 좌우 버튼 동시 드래그 | 패닝 |
| Alt + 좌클릭 드래그 | 기존 피벗 주변 회전 |
| Alt + 우클릭 드래그 | 피벗 거리 변경 |
| Alt + 가운데 버튼 드래그 | 패닝 |

물리 키 코드 사용으로 한글 입력 상태에서도 WASD/QE가 동작합니다. 버튼 해제, 포커스 이탈, 탭 숨김, 지도 비활성화 시 이동을 종료합니다. 입력 필드·마커·링크 등은 카메라 드래그에서 제외합니다.

기준: [Epic Games 뷰포트 컨트롤](https://dev.epicgames.com/documentation/en-us/unreal-engine/viewport-controls-in-unreal-engine).
Google 3D 카메라 모델의 tilt 0–90° 제한을 유지합니다. 언리얼의 모든 에디터 단축키, F로 액터 포커스, 자유로운 천정 방향 비행을 복제한 것은 아닙니다. 터치 제스처는 기존 SDK 입력을 유지합니다.

## 검수 결과

- `node --test tests/*.test.mjs`: **55 통과, 실패 0**.
- 기존 지도 전환, 구성 파일, 배포 허용 파일 목록, JSON 검증, 키 이동 충돌, 곡선, 재생, 취소/복구 테스트 통과.
- 신규 카메라 이동 수학, 우클릭/Alt 모드, 키 입력 조건, 해제/비활성화 종료, 휠 속도와 컨트롤 제외 테스트 통과.
- 확대 좌표 왕복 및 트랙 이름 선택 회귀 테스트 통과.
- 로컬 브라우저 mock fixture: 열기, 숫자 변경, 5초 키 추가, 그래프 전환, 선형 보간(2.5초에서 90°), 실행 취소, 재생, 지도 전환 시 닫기 확인.
- 확대 400%에서 10초 구성이 0–2.5초 구간으로 표시됨을 브라우저에서 확인.
- 운영 사이트의 기존 어스_G 로딩 성공 확인. **수정된 코드의 실제 Google 3D 렌더링/마우스 입력 통합 검증은 아직 하지 않았습니다.** Mock 결과는 실제 SDK 검증을 대신하지 않습니다.

## 적용 상태

이 폴더는 수정된 로컬 체크아웃입니다. 원격 GitHub 저장소 및 Pages에는 아직 반영하지 않았습니다.
`earth-navigation.js`를 배포 파일 목록과 HTML 로드 순서에 추가했습니다. 기존 GitHub Actions 배포 설정은 그대로 사용합니다. 로컬 테스트가 생성한 `_site`는 가짜 테스트 설정이므로 배포하지 마세요.

배포 후에는 우클릭+WASD/QE, Alt 회전, 마커 클릭, 타임라인 재생 중 수동 입력, 지도 전환을 실제 3D 환경에서 추가 검수해야 합니다.
