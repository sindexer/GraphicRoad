# 타임라인 수정 및 Google 기본 조작 복원

## 안정된 지도 렌더와 접이식 타임라인
- 렌더 때마다 새 3D 지도를 만드는 구조를 제거했습니다. 이미 타일 로딩이 끝난 편집 지도를 입력 차단·포커스 해제 상태로 사용하여 사각형 타일 품질 차이와 불필요한 WebGL 장면 중복을 막습니다.
- 출력 비율에 맞춰 지도 뷰포트 자체를 중앙 정렬해 리사이즈하며, 캔버스에서도 종횡비를 보존해 지구가 늘어나지 않습니다.
- 첫 화면 공유 스트림은 페이지 세션 동안 재사용합니다. 브라우저가 공유를 중지하거나 페이지를 새로 열기 전까지 다음 렌더에서는 공유 선택 창을 다시 띄우지 않습니다.
- 렌더 시작 시 타임라인을 자동으로 접고, 실제 프레임 캡처 직전에는 타임라인 전체를 숨기는 clean capture 단계로 전환합니다. `렌더링 중` 표시와 편집 UI는 출력에 포함되지 않습니다.
- 화면 공유 후 포인터를 접힌 타임라인으로 이동한 것이 확인된 뒤 캡처를 시작하여, 브라우저가 합성한 마우스 커서도 지도 출력에서 제외합니다.
- 우측 상단의 닫기 버튼은 아래/위 화살표 접기 버튼으로 변경했습니다.
- 지도와 타임라인 경계를 마우스로 드래그하거나 키보드 위/아래 방향키로 타임라인 높이를 조절할 수 있습니다.

## 편집 도구 개편
- 하단 보기 이동·시간 입력 제거. 시간 눈금 드래그로 재생 위치를 변경합니다.
- 키프레임 추가 버튼은 제목 오른쪽, 재생 버튼과 타임코드는 헤더 중앙에 배치합니다.
- 타임라인에서 Space로 재생/정지합니다. 숫자 입력 중에는 입력을 방해하지 않습니다.
- 빈 트랙 드래그로 박스 선택, Shift로 추가 선택, 선택 키 일괄 이동·삭제를 지원합니다.
- Easy Ease / Ease In / Ease Out은 선택한 키에 일괄 적용되며, 적용 종류를 키에 보존해 첫 키와 마지막 키에서도 아이콘이 정확히 표시됩니다. 상단 버튼과 타임라인은 같은 SVG 경로를 사용합니다.
- `UNDO`/`REDO` 버튼을 명시적으로 표시하고, 지도에 포커스가 있어도 Ctrl+Z, Ctrl+Shift+Z, Ctrl+Y가 동작합니다.
- 확대 후 Shift+휠 또는 가로 휠로 시간축을 이동할 수 있습니다.
- 저장·불러오기·새로 만들기 버튼을 렌더 메뉴로 교체했습니다.
- 렌더 메뉴에서 JPEG 이미지 시퀀스 또는 브라우저가 지원하는 MP4 영상을 저장합니다.
- 자동 테스트 64개 통과. 로컬 브라우저에서 Ease Out 아이콘의 버튼/키 경로 일치와 UNDO 복원을 확인했습니다.

## 유지
- 왼쪽 옵션명·값·키 추가 버튼과 오른쪽 키프레임 시간 막대를 동일한 행으로 정렬.
- 하나의 스크롤 영역, 그래프 전환, 시간축 확대, 키프레임 편집·재생·JSON 입출력.

## 사용자 요청으로 제거
- 언리얼 방식의 우클릭 회전, WASD/QE 비행, Alt 조작 구현.
- 사용자 정의 마우스/키보드 이벤트 가로채기와 카메라 이동 루프.
- 해당 스크립트의 HTML 로딩 및 배포 파일 등록, 관련 테스트.
- 우클릭 점프 수정을 위해 변경했던 카메라 속성 대입 순서.

카메라 조작은 기존 Google Maps 3D 기본 동작으로 복원했습니다.
타임라인의 카메라 키프레임 기능은 그대로 유지합니다.

검증 명령: node --test tests/*.test.mjs
기본 조작 유지 및 커스텀 스크립트 미포함 회귀 테스트를 추가했습니다.
